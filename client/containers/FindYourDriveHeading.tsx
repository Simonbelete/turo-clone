import React, { ReactElement } from 'react';

import { Heading } from 'components';

const FindYourDriveHeadingContainer = (): ReactElement => {
  return (
    <div>
      <Heading className="font-freight font-black">Find your drive</Heading>
    </div>
  );
};

export default FindYourDriveHeadingContainer;
